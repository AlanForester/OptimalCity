<!DOCTYPE html>
<html>
<head>
  <title>Прогноз погоды</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-8">
        <h1 class="text-4xl font-bold text-gray-900 mb-2">Прогноз погоды</h1>
        <p class="text-lg text-gray-600">Текущие показания температуры воздуха на <span id="current-date">...</span></p>
        <p class="text-sm text-gray-500 mt-1">Таймзона: <span id="user-timezone">...</span></p>
        <p class="text-xs text-gray-400 mt-1">Последнее обновление: <span id="last-update">...</span></p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8" id="cities-container">
        <% @cities.each_with_index do |city, index| %>
          <div class="bg-white rounded-lg shadow-lg overflow-hidden">
            <div class="bg-gradient-to-r from-blue-500 to-blue-600 px-6 py-4">
              <h2 class="text-2xl font-semibold text-white"><%= city %></h2>
            </div>
            
            <div class="p-6" id="city-<%= index %>-data" data-city="<%= city %>">
              <div class="text-center py-8">
                <div class="text-gray-400 text-6xl mb-4">🌤️</div>
                <p class="text-gray-500">Загрузка данных...</p>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <script>
    // Получаем таймзону браузера
    function getUserTimezone() {
      try {
        return Intl.DateTimeFormat().resolvedOptions().timeZone;
      } catch (e) {
        return 'UTC';
      }
    }

    // Форматируем дату в локальном формате
    function formatLocalDate(dateString) {
      const date = new Date(dateString);
      return date.toLocaleDateString('ru-RU');
    }

    // Форматируем время в локальном формате
    function formatLocalTime(timestamp) {
      const date = new Date(timestamp);
      return date.toLocaleTimeString('ru-RU', {
        hour: '2-digit',
        minute: '2-digit',
        second: '2-digit'
      });
    }

    // Отображаем данные для города
    function renderCityData(cityName, data, containerId) {
      const container = document.getElementById(containerId);
      
      if (!data || data.length === 0) {
        container.innerHTML = `
          <div class="text-center py-8">
            <div class="text-gray-400 text-6xl mb-4">🌤️</div>
            <p class="text-gray-500">Данные о погоде пока не загружены</p>
            <p class="text-sm text-gray-400 mt-2">Проверьте работу сервиса получения данных</p>
          </div>
        `;
        return;
      }

      // Группируем данные по времени (с точностью до минуты)
      const groupedData = {};
      data.forEach(item => {
        const date = new Date(item.timestamp);
        const timeKey = date.toLocaleTimeString('ru-RU', {
          hour: '2-digit',
          minute: '2-digit'
        });
        
        if (!groupedData[timeKey]) {
          groupedData[timeKey] = [];
        }
        groupedData[timeKey].push(item);
      });

      let html = '<div class="space-y-3">';
      
      // Сортируем ключи времени в обратном порядке (новые сначала)
      const sortedTimes = Object.keys(groupedData).sort((a, b) => {
        const timeA = new Date(`2000-01-01 ${a}`);
        const timeB = new Date(`2000-01-01 ${b}`);
        return timeB - timeA;
      });
      
      sortedTimes.forEach(timeKey => {
        const items = groupedData[timeKey];
        const latestItem = items[0]; // Берем самую свежую запись
        const tempClass = latestItem.temperature >= 0 ? 'text-red-600' : 'text-blue-600';
        
        // Показываем количество записей, если их больше одной
        const countBadge = items.length > 1 ? 
          `<span class="ml-2 px-2 py-1 bg-blue-100 text-blue-800 text-xs rounded-full">${items.length}</span>` : '';
        
        html += `
          <div class="flex justify-between items-center p-3 bg-gray-50 rounded-lg">
            <div class="flex items-center space-x-3">
              <div class="w-3 h-3 bg-blue-500 rounded-full"></div>
              <span class="text-gray-700 font-medium">
                ${timeKey}${countBadge}
              </span>
            </div>
            <div class="text-right">
              <span class="text-2xl font-bold ${tempClass}">${latestItem.temperature}°C</span>
            </div>
          </div>
        `;
      });
      
      html += '</div>';
      container.innerHTML = html;
    }

    // Загружаем данные о погоде
    async function loadWeatherData() {
      const timezone = getUserTimezone();
      
      // Обновляем отображение таймзоны
      document.getElementById('user-timezone').textContent = timezone;
      
      try {
        const response = await fetch(`/api/v1/weather?timezone=${encodeURIComponent(timezone)}`);
        const result = await response.json();
        
        if (result.success) {
          // Обновляем дату
          document.getElementById('current-date').textContent = result.user_date;
          
          // Обновляем время последнего обновления
          document.getElementById('last-update').textContent = new Date().toLocaleTimeString('ru-RU');
          
          // Отображаем данные для каждого города
          Object.keys(result.data).forEach(cityName => {
            const containers = document.querySelectorAll('[data-city="' + cityName + '"]');
            containers.forEach(container => {
              renderCityData(cityName, result.data[cityName], container.id);
            });
          });
        } else {
          console.error('Ошибка загрузки данных:', result.error);
          // Показываем ошибку для всех городов
          document.querySelectorAll('[id^="city-"][id$="-data"]').forEach(container => {
            container.innerHTML = '<div class="text-center py-8"><p class="text-red-500">Ошибка загрузки данных</p></div>';
          });
        }
      } catch (error) {
        console.error('Ошибка запроса:', error);
        // Показываем ошибку для всех городов
        document.querySelectorAll('[id^="city-"][id$="-data"]').forEach(container => {
          container.innerHTML = '<div class="text-center py-8"><p class="text-red-500">Ошибка соединения</p></div>';
        });
      }
    }

    // Загружаем данные при загрузке страницы
    document.addEventListener('DOMContentLoaded', function() {
      loadWeatherData();
      
      // Обновляем данные каждые 5 минут
      setInterval(loadWeatherData, 5 * 60 * 1000);
    });
  </script>
</body>
</html>
